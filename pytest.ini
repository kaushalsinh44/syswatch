[pytest]
# The default OS temp dir (C:\Users\...\Temp\pytest-of-<user>) has hit ACL/permission
# issues on this machine (Access denied scanning it) -- use a project-local temp dir
# instead, which sidesteps that entirely. Gitignored, safe to delete anytime.
addopts = --basetemp=tests/.tmp
testpaths = tests
